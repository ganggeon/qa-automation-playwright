# 결함 리포트 (Defect Report)

| 항목 | 내용 |
|---|---|
| 대상 | MiniShop v1.0 / restful-booker (외부 공개 API) |
| 테스트 기간 | 2026-08-03 |
| 총 검출 결함 | **21건** (MiniShop 16건 + 외부 API 5건) |
| 기준 문서 | `sut/SPEC.md`, [restful-booker API Docs](https://restful-booker.herokuapp.com/apidoc/) |

---

## 결함 현황 요약

| 심각도 | 건수 | 결함 ID |
|---|---:|---|
| **Critical** | 5 | BUG-004, BUG-005, BUG-013, BUG-014, BUG-015 |
| **Major** | 7 | BUG-008, BUG-009, BUG-010, BUG-011, BUG-012, **BUG-016**, EXT-BUG-005 |
| **Minor** | 9 | BUG-001, 002, 003, 006, 007, EXT-BUG-001~004 |

| 기능 영역 | 건수 |
|---|---:|
| 주문/금액 계산 | 5 |
| 입력값 검증 | 5 |
| 인증/인가 | 4 |
| 장바구니 | 4 |
| 상품 조회 | 2 |
| 출력 인코딩 | 1 |

> **BUG-016은 주입 결함(정답지)에 없던 결함이다.** 나머지 15건은 SUT 제작 시 의도적으로
> 심은 것이지만, BUG-016은 명세(FR-06-4)를 문장 단위로 쪼개 읽다가 발견했다.
> 상세는 아래 C절, 발견 경위는 [`sut/SEEDED-DEFECTS.md`](../sut/SEEDED-DEFECTS.md) 마지막 절 참고.

---

# A. Critical — 릴리스 차단 (P1)

## BUG-004 · 계정 잠금 기능이 전혀 동작하지 않는다

| 항목 | 내용 |
|---|---|
| **심각도 / 우선순위** | Critical / P1 |
| **위반 요구사항** | FR-02-3 |
| **기능 영역** | 인증 |
| **발견 TC** | TC-API-LOGIN-005, TC-API-LOGIN-006, TC-UI-LOGIN-003 |
| **상태** | New |

### 재현 절차
1. `POST /api/users` 로 신규 계정을 만든다 (`qa@test.com` / `Qa123456`)
2. `POST /api/auth/login` 에 **틀린 비밀번호**로 5회 연속 요청한다
3. 6번째에 **올바른 비밀번호**로 로그인을 시도한다

### 기대 결과
```
HTTP 423 Locked
{ "error": { "code": "ACCOUNT_LOCKED", "message": "..." } }
```

### 실제 결과
```
HTTP 200 OK
{ "token": "tok_7_a1b2c3d4", "user": { "id": 42, "email": "qa@test.com" } }
```

정상 로그인된다. 초기 데이터의 `locked@test.com`(잠금 상태로 생성된 계정)도 그대로 로그인된다.

### 원인 분석
`sut/server.js` 로그인 핸들러에서 실패 횟수는 누적하고 `locked` 플래그도 세우지만,
**로그인 시작 시점에 `user.locked` 를 확인하는 분기가 없다.** 플래그를 쓰기만 하고 읽지 않는다.

### 영향
무차별 대입(Brute Force) 공격에 대한 방어가 전혀 없다. 계정 잠금은 보안 요구사항이므로
"기능이 조금 이상함"이 아니라 **보안 통제 부재**로 취급해야 한다.

### 제안 수정
```js
// 자격증명 확인보다 먼저 검사해야 한다
if (user && user.locked) {
  return fail(res, 423, 'ACCOUNT_LOCKED', '계정이 잠겼습니다. 관리자에게 문의하세요.');
}
```

---

## BUG-005 · 로그아웃한 토큰이 계속 유효하다

| 항목 | 내용 |
|---|---|
| **심각도 / 우선순위** | Critical / P1 |
| **위반 요구사항** | FR-03-1 |
| **기능 영역** | 인증 |
| **발견 TC** | TC-API-LOGOUT-003 |
| **상태** | New |

### 재현 절차
1. 로그인하여 토큰 `T` 를 받는다
2. `POST /api/auth/logout` (Authorization: Bearer T) → 204 확인
3. 같은 토큰 `T` 로 `GET /api/cart` 를 호출한다

### 기대 결과 / 실제 결과
| | 응답 |
|---|---|
| 기대 | `401 UNAUTHORIZED` |
| 실제 | `200 OK` — 장바구니가 정상 조회됨 |

### 원인 분석
로그아웃 핸들러가 `sessions.delete(req.token)` 를 호출하지 않고 204만 반환한다.
서버는 아무 상태도 바꾸지 않으므로 사실상 **로그아웃이 없는 것과 같다.**

### 영향
공용 PC에서 로그아웃해도 세션이 살아있다. 탈취된 토큰을 무효화할 수단이 없다.

---

## BUG-013 · 할인액이 주문금액보다 크면 결제금액이 음수가 된다

| 항목 | 내용 |
|---|---|
| **심각도 / 우선순위** | Critical / P1 |
| **위반 요구사항** | FR-09-5 |
| **기능 영역** | 주문/금액 |
| **발견 TC** | TC-API-ORDER-003, TC-UI-E2E-003 |
| **상태** | New |

### 재현 절차
1. 로그인 후 `북마크 세트`(2,345원) 1개를 장바구니에 담는다
2. `POST /api/orders` 에 `{ "couponCode": "BIG50000" }` (50,000원 할인)

### 기대 결과 / 실제 결과
| 항목 | 기대 | 실제 |
|---|---:|---:|
| totalPrice | 2,345 | 2,345 |
| discount | 2,345 (총액까지만) | 50,000 |
| **finalPrice** | **0** | **-47,655** |

UI에서도 `주문이 완료되었습니다. 주문번호 5 / 결제금액 -47,655원` 이 그대로 노출된다.

### 영향
결제 연동 시 **환불이 아니라 고객에게 돈을 지급하는** 상황이 될 수 있다.
금전적 손실이 직접 발생하므로 최우선 수정 대상이다.

### 제안 수정
```js
const discount = Math.min(rawDiscount, view.totalPrice);
const finalPrice = Math.max(0, view.totalPrice - discount);
```

---

## BUG-014 · 재고 확인 없이 차감하여 재고가 음수가 된다

| 항목 | 내용 |
|---|---|
| **심각도 / 우선순위** | Critical / P1 |
| **위반 요구사항** | FR-09-1, FR-09-2 |
| **기능 영역** | 주문/재고 |
| **발견 TC** | TC-API-ORDER-005, TC-API-ORDER-006 |
| **상태** | New |

### 재현 절차
1. `GET /api/products/9` 로 재고 확인 (초기 재고 4)
2. 해당 상품을 **10개** 장바구니에 담는다
3. `POST /api/orders` 로 주문한다
4. `GET /api/products/9` 로 재고를 다시 확인한다

### 기대 결과 / 실제 결과
| | 주문 응답 | 주문 후 재고 |
|---|---|---:|
| 기대 | `409 OUT_OF_STOCK` | 4 (변동 없음) |
| 실제 | `201 Created` | **-6** |

### 영향
- 없는 재고가 팔린다 → 배송 불가 주문 발생, 고객 클레임
- 재고 데이터 무결성이 깨져 이후 모든 재고 판단이 어긋난다
- 응답 스키마 검증(`stock: minimum 0`)에서도 동시에 걸린다

### 참고
BUG-009(담기 시점 재고 미확인)와 원인은 같은 계열이지만 **수정 위치가 다르다.**
담기에서 막아도 주문 시점에 재고가 줄어 있을 수 있으므로 **주문 시점 검증이 반드시 필요하다.**
두 곳 모두 수정해야 한다.

---

## BUG-015 · 상품명이 이스케이프 없이 렌더링된다 (저장형 XSS)

| 항목 | 내용 |
|---|---|
| **심각도 / 우선순위** | Critical / P1 |
| **위반 요구사항** | UI-01 |
| **기능 영역** | 출력 인코딩 / 보안 |
| **발견 TC** | TC-UI-PROD-002, TC-UI-PROD-003 |
| **상태** | New |

### 재현 절차
1. 로그인 후 상품 목록 화면(`/#/products`)으로 이동한다
2. 상품 ID 4 (`HTML <div> 완전정복`) 의 상품명 표시를 확인한다

### 기대 결과 / 실제 결과
| | 화면 표시 | DOM |
|---|---|---|
| 기대 | `HTML <div> 완전정복` | `<td>HTML &lt;div&gt; 완전정복</td>` |
| 실제 | `HTML  완전정복` (태그가 사라짐) | `<td>HTML <div> 완전정복</div></td>` |

### 원인 분석
`sut/public/index.html` 의 `loadProducts()` 가 서버 데이터를 문자열로 이어붙여
`innerHTML` 에 대입한다. 데이터가 **마크업으로 해석**된다.

### 영향
- **표시 오류**: 특수문자가 포함된 상품명이 잘못 보인다 (즉시 확인 가능한 증상)
- **보안**: 상품명에 `<img src=x onerror="...">` 같은 값이 저장되면 그 목록을 보는
  **모든 사용자의 브라우저에서 스크립트가 실행된다** (저장형 XSS). 세션 토큰 탈취로 이어질 수 있다

### 제안 수정
`innerHTML` 문자열 조립 대신 `textContent` 로 값을 넣거나, 삽입 전에 `<`, `>`, `&`, `"` 를 이스케이프한다.

---

# B. Major — 이번 릴리스 내 수정 (P2)

## BUG-008 · 장바구니 수량 하한 검증 누락 → 음수 총액

| 항목 | 내용 |
|---|---|
| **심각도 / 우선순위** | Major / P2 |
| **위반 요구사항** | FR-06-2 |
| **발견 TC** | TC-API-CART-002, TC-API-CART-006 |

**재현**: 로그인 후 `POST /api/cart` 에 `{ "productId": 1, "qty": -3 }`

| | 응답 | 장바구니 총액 |
|---|---|---:|
| 기대 | `400 INVALID_QTY` | — |
| 실제 | `201 Created` | **-99,000원** |

**원인**: `if (!Number.isInteger(qty) || qty > 99)` — 상한만 검사하고 하한(`qty < 1`)이 빠졌다.
**영향**: 음수 수량으로 총액을 조작해 다른 상품을 무료로 결제할 수 있다. 금액 계산의 신뢰가 무너진다.

---

## BUG-009 · 담기 시점에 재고를 확인하지 않는다

| 항목 | 내용 |
|---|---|
| **심각도 / 우선순위** | Major / P2 |
| **위반 요구사항** | FR-06-3 |
| **발견 TC** | TC-API-CART-007, TC-API-CART-008, TC-UI-PROD-008 |

**재현**
- (a) 품절 상품(ID 3, 재고 0)을 1개 담기 → 기대 `409`, 실제 `201`
- (b) 재고 2개 상품(ID 15)을 5개 담기 → 기대 `409`, 실제 `201`

UI에서도 품절 상품에 대해 `장바구니에 담았습니다.` 성공 메시지가 표시된다.

**영향**: 사용자가 결제 직전까지 진행한 뒤에야 실패를 알게 된다. 이탈률에 직결된다.

---

## BUG-010 · 존재하지 않거나 남의 장바구니 항목을 삭제해도 성공(204)을 반환한다

| 항목 | 내용 |
|---|---|
| **심각도 / 우선순위** | Major / P2 |
| **위반 요구사항** | FR-08-1, FR-08-2 |
| **발견 TC** | TC-API-CART-016, TC-API-CART-017 |

**재현**
- (a) `DELETE /api/cart/999999` (존재하지 않는 항목) → 기대 `404`, 실제 `204`
- (b) A 계정으로 담은 itemId를 B 계정 토큰으로 삭제 → 기대 `404`, 실제 `204`

**원인**: `findIndex` 결과가 -1이어도 무조건 204를 반환한다. 소유자 확인 자체가 없다.

**영향**
- 클라이언트가 삭제 성공을 신뢰할 수 없다
- (b)는 **인가(Authorization) 검증 누락**이다. 이번에는 삭제 대상이 없어 실제 피해가 없었지만,
  같은 패턴이 다른 API에 복제되면 남의 데이터를 지우는 취약점이 된다

---

## BUG-011 · 쿠폰 최소 주문금액을 "초과"로 비교한다

| 항목 | 내용 |
|---|---|
| **심각도 / 우선순위** | Major / P2 |
| **위반 요구사항** | FR-09-3 |
| **발견 TC** | TC-API-ORDER-001 |

**재현**: `Java의 정석`(30,000원) 1개 주문 + 쿠폰 `SAVE5000`(최소 주문금액 30,000원)

| | 응답 |
|---|---|
| 기대 | `201` — 최소금액 "이상"이므로 5,000원 할인 적용 |
| 실제 | `400 COUPON_MIN_AMOUNT` |

**원인**: `if (view.totalPrice <= c.minAmount)` — `<` 여야 하는데 `<=` 로 되어 있다.

**영향**: 정확히 최소금액을 맞춘 고객이 할인을 못 받는다. 프로모션 문의·클레임으로 직결된다.
문구가 "30,000원 이상 구매 시"이므로 **고객 입장에서는 명백한 계약 위반**이다.

---

## BUG-012 · 정률 할인액을 절사하지 않아 결제금액에 소수점이 남는다

| 항목 | 내용 |
|---|---|
| **심각도 / 우선순위** | Major / P2 |
| **위반 요구사항** | FR-09-6 |
| **발견 TC** | TC-API-ORDER-002 |

**재현**: `북마크 세트`(2,345원) 1개 + 쿠폰 `WELCOME10`(10%)

| 항목 | 기대 | 실제 |
|---|---:|---:|
| discount | 234 | **234.5** |
| finalPrice | 2,111 | **2,110.5** |

**원인**: `discount = (totalPrice * value) / 100` — `Math.floor()` 가 없다.
**영향**: 원화는 소수점 단위가 없다. PG 연동 시 금액 불일치로 결제가 거부되거나
반올림 처리가 시스템마다 달라져 정산 차이가 누적된다.

---

## BUG-016 · 장바구니 합산 시 수량 상한을 다시 검사하지 않는다 ★ 정답지에 없던 결함

| 항목 | 내용 |
|---|---|
| **심각도 / 우선순위** | Major / P2 |
| **위반 요구사항** | FR-06-4 (및 그 결과로 FR-06-2) |
| **기능 영역** | 장바구니 |
| **발견 TC** | TC-API-CART-018 |
| **발견 기법** | 경계값 분석 — "합산 **이후**의 값"에도 경계가 있다 |
| **상태** | New |

### 발견 경위

FR-06-4 는 한 문장이지만 조건이 두 개다.

> 이미 담긴 상품이면 수량을 합산한다 **(합산 결과도 FR-06-2/3 규칙을 지켜야 함)**

앞 절반(합산된다)만 검증하는 TC는 이미 있었다(TC-API-CART-010). 괄호 안의 뒷 절반은
아무도 검증하지 않고 있었다. **경계는 입력값에만 있는 게 아니라 계산 결과에도 있다.**

### 재현 절차

1. 로그인 후 `연습장 100매`(id 16, 재고 999)를 **99개** 담는다 → `201`
2. 같은 상품을 **1개** 더 담는다 (합산하면 100개)

### 기대 결과 / 실제 결과

| | 응답 | 장바구니 수량 |
|---|---|---:|
| 기대 | `400 INVALID_QTY` | 99 (변화 없음) |
| 실제 | **`201 Created`** | **100** |

재고 999짜리 전용 상품을 쓰므로 재고 규칙(FR-06-3)에는 걸리지 않는다.
즉 여기서 드러나는 위반은 **오직 수량 상한(FR-06-2)** 하나다.

### 원인 분석

```js
// sut/server.js — 담기 핸들러
if (!Number.isInteger(qty) || qty > 99) { ... }   // ← 요청값 하나만 검사
...
if (exist) exist.qty += qty;                      // ← 합산 결과는 재검사하지 않음
```

검증이 **요청 시점**에만 걸려 있고 **상태 변경 시점**에는 없다.
BUG-008(하한 누락)과 원인이 비슷해 보이지만 다르다. BUG-008은 검사식 자체가 빠진 것이고,
이건 검사식은 있는데 **적용 지점이 틀린 것**이다.

### 영향

수량 상한을 두는 이유(1인당 구매 제한, 재고 확보, 어뷰징 방지)가 무력화된다.
요청을 여러 번 나눠 보내면 상한을 얼마든지 우회할 수 있고, 재고 검증이
수정되더라도 이 경로는 그대로 남는다.

### 제안 수정

```js
const merged = (exist?.qty ?? 0) + qty;
if (!Number.isInteger(qty) || merged < 1 || merged > 99) {
  return fail(res, 400, 'INVALID_QTY', '수량은 1 이상 99 이하의 정수여야 합니다.');
}
if (merged > product.stock) {
  return fail(res, 409, 'OUT_OF_STOCK', '재고가 부족합니다.');
}
```

> **왜 이 한 건이 중요한가.** 나머지 15건은 "심어둔 것을 찾은" 결과라 검출률이
> 실력을 증명하지 못한다. 이 건은 정답지에 없었고, 명세를 문장 단위로 쪼개 읽는
> 과정에서만 나왔다. **명세 기반 설계가 실제로 작동한다는 유일한 자체 증거다.**

---

## EXT-BUG-005 · [restful-booker] 필수 필드 누락 시 500 에러

| 항목 | 내용 |
|---|---|
| **심각도 / 우선순위** | Major / P2 |
| **발견 TC** | TC-EXT-VAL-002 |

**재현**: `POST /booking` 에 `{ "firstname": "OnlyName" }` 만 전송

| | 응답 |
|---|---|
| 기대 | `400 Bad Request` + 어떤 필드가 빠졌는지 안내 |
| 실제 | **`500 Internal Server Error`** + `Internal Server Error` |

**분석**: 입력값 검증 없이 처리하다 서버 내부에서 예외가 터진 것이다.
클라이언트 실수(4xx)를 서버 장애(5xx)로 응답하면 **모니터링 알람이 오염되고**,
호출자는 재시도해야 할지 요청을 고쳐야 할지 판단할 수 없다.

---

# C. Minor — 차기 릴리스 반영 가능 (P3)

| ID | 요약 | 위반 | 재현 요약 | 기대 → 실제 | TC |
|---|---|---|---|---|---|
| **BUG-001** | 비밀번호 최대 길이(20자) 검증 누락 | FR-01-3 | 21자 비밀번호로 가입 | `400` → `201` | SIGNUP-006 |
| **BUG-002** | 나이 상한(120) 검증 누락 | FR-01-5 | `age: 121` 로 가입 | `400` → `201` | SIGNUP-010 |
| **BUG-003** | 이메일 정규식이 느슨함 | FR-01-1 | `a@b` 로 가입 (도메인에 점 없음) | `400` → `201` | SIGNUP-014 |
| **BUG-006** | `size=0` 이 거부되지 않고 전체 목록 반환 | FR-04 | `GET /api/products?size=0` | `400` → `200` (전체 목록) | PROD-002 |
| **BUG-007** | 상품 검색이 대소문자를 구분함 | FR-04 | `keyword=java` 검색 | 'Java의 정석' 조회됨 → 0건 | PROD-013/014 |

> **BUG-006이 왜 Minor인가**: 데이터가 새거나 망가지지 않고, 정상 사용자는 `size=0`을 보내지 않는다.
> 다만 페이지네이션 계약이 깨져 클라이언트 구현이 어긋날 수 있으므로 수정은 필요하다.
> **BUG-003도 Minor로 판단했다**: 잘못된 형식의 이메일로 가입되면 발송 실패가 발생하지만
> 즉각적인 데이터 손상이나 보안 문제는 아니다.

## 외부 API (restful-booker) — 참고용

| ID | 요약 | 기대 → 실제 | 분석 | TC |
|---|---|---|---|---|
| **EXT-BUG-001** | 인증 실패에 200 OK 반환 | `401` → `200 {"reason":"Bad credentials"}` | 상태코드만 확인하는 클라이언트는 실패를 성공으로 오인한다 | EXT-AUTH-002 |
| **EXT-BUG-002** | 리소스 생성에 200 반환 | `201` → `200` | REST 규약 위반. 생성 여부를 상태코드로 구분할 수 없다 | EXT-BOOK-001 |
| **EXT-BUG-003** | 삭제 성공에 `201 Created` 반환 | `204` → `201 "Created"` | 삭제인데 "Created" 라는 의미상 모순. 본문도 JSON이 아닌 평문 | EXT-BOOK-006 |
| **EXT-BUG-004** | 체크아웃이 체크인보다 빠른 예약이 생성됨 | `400` → `200` | 날짜 순서 검증이 전혀 없다. 잘못된 데이터가 DB에 그대로 저장된다 | EXT-VAL-001 |

---

# D. 회귀 방지 방안

각 결함마다 대응하는 자동화 테스트케이스가 이미 존재하며,
`test.fail()` 로 **"알려진 결함"** 이라 표시해 두었다.

```ts
test('TC-API-ORDER-003 | 할인액이 주문금액보다 커도 결제금액은 0원 미만이 되지 않는다', async ({ shop }) => {
  test.fail(true, 'BUG-013: 결제금액 하한 처리가 없어 음수 결제금액이 생성된다');
  ...
});
```

이 방식의 이점

1. **CI가 초록불로 유지된다** — 미수정 결함 때문에 매일 빨간불이 뜨면 팀이 실패에 무뎌진다
2. **결함이 수정되면 자동으로 알려준다** — 수정 후 테스트가 통과하면
   Playwright가 `Expected to fail, but passed` 로 실패시킨다. 마킹을 지우라는 신호다
3. **결함 목록이 코드에 산다** — 별도 문서와 코드가 따로 놀지 않는다

수정 완료 후 절차
1. `test.fail()` 한 줄 삭제
2. 전체 스위트 재실행 (`npm test`)
3. 통과하면 결함 상태를 `Verified → Closed` 로 변경
